---
title: Fixed Size Examples
---

Remember to import the `<Grid />` component. It is not shown in the examples below.

```js
import { Grid } from "@resembli/le-window"
```

For more on the `<Grid />` component, please refer to the component's [API documentation](/docs/le-window/grid)

## Basic Example

```js live
function GridBasicExample() {
  const gridData = Array(1000)
    .fill(0)
    .map((_, row) => {
      const cells = Array(200)
        .fill(0)
        .map((_, column) => {
          return { row, column }
        })

      return { cells }
    })

  function getRandomHEXColor() {
    const SEED = "0123456789abcdef"
    let output = "#"
    while (output.length < 7) {
      output += SEED[Math.floor(Math.random() * SEED.length)]
    }
    return output
  }

  return (
    <div style={{ height: 600 }}>
      <Grid data={gridData} defaultColumnWidth={100} defaultRowHeight={100}>
        {({ row, column }, styles) => {
          const startColor = getRandomHEXColor()
          const endColor = getRandomHEXColor()

          return (
            <div
              style={{
                justifyContent: "center",
                alignItems: "center",
                height: "100%",
                background: `linear-gradient(to right, ${startColor}, ${endColor}`,
                color: "black",
                ...styles,
                display: "inline-flex",
              }}
            >
              ({row}, {column})
            </div>
          )
        }}
      </Grid>
    </div>
  )
}
```
